# Page builder — tracked gap inventory

Every row's `Constrained at` cites a real `file:line`, opened and confirmed against this
worktree at branch point `b3544283` (2026-09-13). This doc is re-read by Task 12, which ticks
rows off — do not renumber IDs once a later task references one.

`Status` is one of `open` / `closing in 2026-09-13 build` / `closed`.

## Design gaps

These are all things the *2026-09-13 build* (this branch) is closing. They exist because
`prompt.ts`'s stylesheet-decides-everything model was correct when the engine shipped and has
since been paid for long enough to see the cost: the model is handed a design surface small
enough that most pages come out looking the same.

| ID | Area | What the owner cannot do | Constrained at | Severity | Status |
|---|---|---|---|---|---|
| G1 | Theme | Choose from more than 12 total page looks — `theme.tone`(2) × `theme.accent`(2) × `theme.radius`(3) | `lib/funnels/sections/registry.ts:167` | High | closed |
| G2 | Colour | Set any hex/colour value anywhere in the document — the prompt tells the model outright that "no hex or colour field exists anywhere in this document" | `lib/funnels/sections/prompt.ts` `BUILDER_RULES` rule 6 | High | closed |
| G3 | Typography | Choose a font; heading/body font-family are hardcoded to Lexend Exa / Lexend Deca with no document-level override | `lib/funnels/sections/styles.ts:352` | Medium | closed |
| G4 | Layout | Change the page container width; every section's content is capped at a fixed `max-width: 72rem` | `lib/funnels/sections/styles.ts:191` | Medium | closed |
| G5 | Section style | Right-align a section; `align` only offers `left` / `center` | `lib/funnels/sections/registry.ts:143` | Low | closed |
| G6 | Section style | Give a section its own background colour/image or an overlay — `sectionStyleSchema` has no such field | `lib/funnels/sections/registry.ts:141` | Medium | closed |
| G7 | Section style | Add a visual divider between two sections — `sectionStyleSchema` has no such field | `lib/funnels/sections/registry.ts:141` | Low | closed |
| G8 | Variants | Flip a split layout (which side the pitch/media sits on); `.djp-v-split` CSS hardcodes the order both for `hero` and `form` | `lib/funnels/sections/styles.ts:964` | Low | closed |
| G9 | Variants | Get a second look for `faq` or `quiz` — each has exactly one variant | `lib/funnels/sections/registry.ts:360,424` | Medium | closed |
| G10 | Variants | Get a third look for `proof`, `steps`, `testimonial`, `pricing`, `cta` or `footer` — each has exactly two | `lib/funnels/sections/registry.ts:246,286,309,335,440,454` | Low | closed |
| G11 | Media | Put an image anywhere except the hero section — `heroMediaSchema` is the document's only media-carrying field | `lib/funnels/sections/registry.ts:223` | Medium | closed |
| G12 | Page shape | Get anything other than the fixed leadgen skeleton (form first, `variant:"split"`, proof near the top, thin footer) regardless of what the page is actually for — `LEADGEN_RULES` prescribes one shape for every page | `lib/funnels/sections/prompt.ts:521` | High | closed |
| G13 | Variation | Get a different page from an identical brief — the first-draft seed document is one fixed literal (`theme: light/accent/soft`, a single footer placeholder) and nothing in the turn carries a variation input | `app/api/admin/funnels/steps/[stepId]/build/route.ts:191` | Medium | closed |
| G14 | Colour | Trust a brand-coloured heading, price or stat (`.djp-hd` / `.djp-plan-price` / `.djp-proof-value`, painted `var(--primary)` at the default, untoned section) to actually be readable on a generated palette — `resolvePalette` guaranteed ink-vs-surface and ink-vs-paper (`pickInk`, `deriveSurface`) but never brand-vs-paper, because `brand` is only proven as a BACKGROUND paired with `brandInk`, not as text on the page ground. Measured before the fix: 4 of 12 presets failed even the 3:1 large-text floor (`ink` worst at 1.10:1; `midnight` 1.88, `steel` 2.19, `plum` 2.74), and the one preset that passed (`ember`) did so by 0.01 (3.007:1) — confirmed live on `/preview/camp-kfcsg`'s FAQ heading. Closed by `deriveBrandOnPaper` (`lib/funnels/sections/palettes.ts:268`), a new `brandOnPaper` token that keeps brand's hue and nudges lightness until it clears 4.5:1 against BOTH paper and surface — guaranteed for all twelve presets (worst case now `ember` at 4.510:1) and for `resolvePalette`'s full hue wheel in both light and dark mode (`__tests__/lib/funnels/sections/palettes.test.ts`). `.djp-hd`/`.djp-plan-price`/`.djp-proof-value` now read `var(--primary-on-paper, var(--primary))`; every rule that paints `brand` as a background (the `dark`-tone band, badges) is untouched | `lib/funnels/sections/palettes.ts:268` (`deriveBrandOnPaper`) | High | closed |
| G15 | Colour | Trust an eyebrow, icon, required-field marker, FAQ toggle glyph, or quiz progress bar (`.djp-eyebrow` / `.djp-ic` / `.djp-req` / the FAQ "+"/"−" / `.djp-quiz-progress-bar`, all painted `var(--accent)` at the default, untoned section) to actually be readable — the identical gap as G14, for the OTHER brand token, missed by the first fix wave's tone-contrast sweep (`styles.ts:493-502`) because that sweep covered the accent/dark/muted TONES but never the untoned default ground `--accent` itself sits on. Measured before this fix: `ink` 1.11:1, `steel` 1.40:1, `midnight` 2.47, `sand` 2.59, `clay` 3.55, `bone` 4.57 — same order of magnitude as G14. Found by the final whole-branch review (finding 1, 2026-09-13); the review's own line list under-counted by one (`.djp-s-form.djp-v-split .djp-form-proof .djp-ic` has the identical hazard and was fixed alongside the six named sites). Closed by `deriveAccentOnPaper` (`lib/funnels/sections/palettes.ts`), a new `accentOnPaper` token proven >= 4.5:1 against both paper and surface for all twelve presets (worst case `midnight` at 4.539:1 on surface). The review's deeper finding — `__tests__/lib/funnels/sections/render.test.ts`'s `READABLE_ON` table hand-declared `--accent` "legal" on `--background`/`--surface` as a scope-invariant fact that was only true for `app/globals.css`'s four fixed themes — is also closed: those two entries are now DERIVED from real `contrastRatio` measurements against `PALETTE_TABLE` (`neutralGroundForeground`) rather than hand-listed | `lib/funnels/sections/palettes.ts` (`deriveAccentOnPaper`) | High | closed |
| G16 | Colour | Trust body copy painted `var(--muted-foreground)` (nine classes: `.djp-sub`, `.djp-bullet-text`, `.djp-step-text`, `.djp-plan-blurb`, `.djp-plan-cadence`, `.djp-footnote`, `.djp-quiz-step`, `.djp-footer-line`/`.djp-footer-legal`) to be readable on a dark-seeded palette — `--muted-foreground` is a FIXED app-level token (`app/globals.css`'s bare `:root`, `oklch(0.5 0.01 250)` ≈ `#5f6469`) that no `PALETTE_TABLE` preset derives or overrides, so it never enters `resolvePalette`'s AA guarantee at all — it is outside the palette's guarantee entirely, not merely unproven. Measured against the five dark-seeded presets: `ember` 3.26:1 on paper / 3.16:1 on surface, `ink` 3.26 / 3.24, `midnight` 3.26 / 3.16, `steel` 3.26 / 3.14, `plum` 3.26 / 3.15 — all below the 4.5:1 body-text floor (and below even 3:1 on some surfaces once rounding is accounted for). Deliberately NOT fixed in the same pass as G15: a correct fix means either deriving a palette-aware muted-foreground token (widening `PaletteTokens`/`doc.ts`'s palette block again) or accepting a lower floor for genuinely secondary copy — a real design decision, not a mechanical on-paper derivation like G14/G15 | `app/globals.css:19` (`--muted-foreground` on bare `:root`); consumers e.g. `lib/funnels/sections/styles.ts:538` (`.djp-sub`) | Medium | open |

## Capability gaps

These are explicitly **not** part of the 2026-09-13 build (see spec §9). Logged here so the
owner can see the whole shape of what the builder can't do yet, not just the slice this build
closes.

| ID | Area | What the owner cannot do | Constrained at | Severity | Status |
|---|---|---|---|---|---|
| C1 | Styling | Write free-form CSS for a section — the typed `sectionStyleSchema` is the only style surface, by deliberate choice (a CSS escape hatch would let preview and publish diverge) | `lib/funnels/sections/registry.ts:141` | Medium | open |
| C2 | Section kinds | Add a `gallery`, logo-strip, comparison-table, countdown, guarantee-badge, contact/map or before-after section — `SECTION_KINDS` lists 11 kinds and none of these are among them | `lib/funnels/sections/registry.ts:185` | Medium | open |
| C3 | Input | Paste a reference design or brand board image into chat — `ChatPane.tsx` has no attachment/image path and the build route takes text only | `components/admin/funnels/builder/ChatPane.tsx` (no attachment path anywhere in the file) | Medium | open |
| C4 | Feedback loop | Have the model see its own rendered page; `buildTurnMessage` sends only the pretty-printed JSON doc, never the compiled `{html, css}` | `lib/funnels/sections/prompt.ts:902` (`buildTurnMessage`) | Medium | open |
| C5 | Multi-page edits | Ask for a change that applies across every page of a funnel in one turn — a turn's input is a single step's `SectionDoc`, not the funnel | `lib/funnels/sections/prompt.ts:879` (`BuilderTurnInput.doc`) | Low | open |
| C6 | Funnel structure | Have the model add, reorder or rename funnel steps — `opSchema` is a closed union of section-level ops only (`set_page`, `add_section`, `update_section`, `move_section`, `remove_section`, `set_theme`) | `lib/funnels/sections/apply.ts:117` (`opSchema`) | Low | open |
| C7 | Typography | Use a custom web font beyond the three app-wide fonts (Lexend Exa / Lexend Deca / JetBrains Mono) | `lib/funnels/sections/styles.ts:26` (font caveat comment) and `:186` (hardcoded chain) | Low | open |
| C8 | Media | Generate an image or search stock photography from chat — `heroMediaSchema.src` requires a URL the owner or model must already have; nothing produces one | `lib/funnels/sections/registry.ts:211` (`heroMediaSchema`) | Low | open |
| C9 | Identifiers | Have the model set `eventId`, `quizId`, or any other UUID itself. **This one is `open` and correct by design, not a gap to close** — the publish gate refuses a page whose quiz/event id is missing or invalid, so a model-authored id is a batch that can never publish; only the owner can supply one from the builder | `lib/funnels/sections/prompt.ts:319-329` | N/A — by design | open |
| C10 | Memory | Have the model recall its own past ops beyond the last 8 prose turns of history — history carries prose only, replaying old ops was rejected as redundant and self-reinforcing | `lib/funnels/sections/builder-config.ts:71` (`SECTION_BUILDER_HISTORY_TURNS = 8`) | Low | open |
| C11 | Review pipeline | Get a `section-count` audit that knows which `PAGE_RECIPE` a page was built from. `lib/funnels/sections/review/audit.ts`'s `SECTION_COUNT_MIN`/`SECTION_COUNT_MAX` apply ONE range to every page regardless of recipe — task 10 raised the range to 6..16 (from 6..9) as a stopgap so a deliberately long `long-form-sales` page (prompt.ts: "Length is earned here... more sections is normal") does not get flagged and reverted by the reviser, but 6..16 is a compromise standing in for real per-recipe bounds: `capture-split`/`capture-hero-first`/`event`/`application`/`thank-you` all still want something closer to the old 6..9, and 16 is loose enough to miss real bloat on those. The correct fix — store the chosen recipe on `SectionDoc` and branch the auditor's bound on it — is a schema change (`SectionDoc` has no recipe field today) and is deliberately out of scope here | `lib/funnels/sections/review/audit.ts:264-265,501-511` | Medium | open |
