# local-only handoff/journal docs (never commit)
JOURNAL.md
DEVLOG.md

# dependencies
node_modules/

# firebase functions build output
functions/dist/

# firebase emulator local secrets (defineSecret values)
functions/.secret.local
functions/.env
functions/.env.local
functions/.env.*.local
firebase-debug.log
firestore-debug.log
database-debug.log
pubsub-debug.log
ui-debug.log
.firebase/

# next.js
.next/
out/

# environment
.env
.env.local
.env.*.local
.env.prod
.env.production

# mcp server configs (may contain tokens)
.mcp.json

# debug
npm-debug.log*

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# typescript
*.tsbuildinfo
next-env.d.ts

# testing
coverage/
playwright-report/
test-results/
.playwright-out/

# local artifacts (screenshots, exercise templates, etc.)
Screenshot*.png
*.xlsx

# claude code
.claude/
.superpowers/

# vercel
.vercel

# git worktrees
.worktrees/

.playwright-mcp/

# demo/tutorial videos (local deliverables, never committed)
*.mp4
scratch/

# walkthrough takes are regenerated by the recorder; the EDL is committed
render-worker/public/walkthrough/*.mp4
# client-promo raw take: thousands of 1080x1920 JPEGs, regenerated by
# scripts/record-client-promo.mjs. Only the EDL (config.ts) is committed.
.promo-take/
# narration WAVs are deterministic output of synth-walkthrough-narration.mjs
render-worker/public/walkthrough/audio/

# GHL export dumps (contact PII + consent records — never commit)
ghl-export/

# Raw screenshot captures — intermediates, before annotate() burns the callouts in.
screenshots/**/.raw-*.png
